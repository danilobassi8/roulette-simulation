{
  "name": "roulette",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "build-n-pages": "npm run build && git subtree push --prefix dist origin gh-pages"
  },
  "devDependencies": {
    "vite": "^2.9.5"
  },
  "dependencies": {
    "gsap": "^3.10.4"
  }
}
